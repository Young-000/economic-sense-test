/**
 * 투자자 유형 프로필
 * @deprecated Domain 레이어에서 직접 import하세요: import { investorProfiles } from '@domain/entities'
 */

// Domain에서 re-export (하위 호환성 유지)
export { investorProfiles } from '@domain/entities';
