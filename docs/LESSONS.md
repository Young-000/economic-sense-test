# LESSONS — Economic Sense Test

> 비자명한 교훈만 1줄로. 야간 사이클이 착수 전 읽고 같은 실수를 반복하지 않는다.

## 2026-08-09 주간 (PM 사이클)

- **미런칭 앱에 기능/숫자 튜닝을 쌓지 마라.** 실사용 6개월+ 0인데 최근 4커밋이 코인 이코노미 v3→v3.1 리밸런스였다. 앱은 AIT 콘솔 미제출(외부노출 게이트)이라 런칭 자체가 안 됐고, 조정 대상 이코노미는 DB 테이블(`user_sessions`/`promotion_records`)이 없어 런타임에서 애초에 실행되지 않는다. → **지표가 죽어 있으면 "기능을 더"가 아니라 "왜 런칭이 안 됐나"를 먼저 본다.**
- **"완성도 95%"는 지표가 아니다.** CLAUDE.md는 수개월째 95%인데 실사용 지표는 0. 자기보고 완성도 대신 런칭 여부·실사용을 그로스 신호로 삼는다.
- **마이그레이션 SQL 작성 ≠ 적용.** commit 46f1bde가 `user_sessions`/`promotion_records`를 "추가"했지만 실 DB엔 두 테이블이 없다. 마이그레이션은 실 DB 반영 + 스모크 테스트까지가 완료다.
